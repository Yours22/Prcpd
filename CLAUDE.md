# 项目概述

## 物理背景 — TWIGL 反应堆瞬态问题

本项目的物理核心是**二维中子扩散瞬态模拟**，基于 **TWIGL 基准题**。

### 初始稳态
反应堆初始处于临界状态（中子产生率 = 消失率），快群和热群中子通量在 20×20 的二维空间网格上形成稳定分布。每个网格节点有快群通量和热群通量两个值。

### 瞬态扰动 — 反应性引入事故
通过线性改变特定区域材料的宏观吸收截面（模拟控制棒移动或冷却剂丧失），向堆芯引入正反应性。扰动由 4 个参数完全描述：

| 参数 | 含义 | 取值范围 |
|------|------|---------|
| `material_changing` | 扰动区域 | 1=核心区, 3=控制棒区 |
| `group_changing` | 扰动能群 | 1=快群, 2=热群 |
| `slope_up` | 截面变化斜率（负值, /s） | -0.3 ~ -0.01 |
| `cut_time` | 扰动截止时间（s） | 0.1 ~ 0.4 |

扰动机制：吸收截面线性减小 → 中子倍增因子 k 上升 → 中子通量随时间指数增长。时间范围 t=0 到 t=0.5s，共 101 步（dt=0.005s）。

### 数据生成流程
1. LHS 采样 2500 组扰动参数 → 生成 .prm 输入文件
2. FEMFFUSION 扩散求解器逐算例求解，输出每步的 VTK 通量场（800 维：400 快群节点 + 400 热群节点）
3. 按 `slope_up × cut_time`（总扰动量）排序，将参数空间两端的 148 个极端算例划为外推测试集

### 核心挑战
- **维度灾难**：800 维输出 × 101 时间步 = 巨大的预测空间
- **指数爆发**：瞬态末期通量可能增长数个数量级，传统 MSE 损失会淹没前期细节
- **外推泛化**：测试集位于参数空间边缘（更极端的扰动），需要模型真正学会物理规律而非插值

## 方法架构 — POD-LSTM 振幅-形状分解

### 整体思路
将"从扰动参数预测 800 维通量场时间序列"转化为"从 10 维输入特征预测 8 维 POD 系数时间序列"，再用 POD 模态重构回物理空间。

### POD 降维
- 用 TruncatedSVD 分别对快群和热群通量场降维（各保留 4 个模态 → 共 8 个系数）
- 物理含义：Mode 1 = 全场平均振幅（能量主体），Modes 2-4 = 空间分布形状的精细修正

### 输入特征（10 维）
```
原始 6 维: [is_reg1, is_reg2, is_fast, is_thermal, p_t, t]
指数衰减积分 3 维: ∫ p_t(τ)·e^{-λ(t-τ)} dτ  (λ = 0.1, 1.0, 10.0)
简单累积积分 1 维: ∫ p_t(τ) dτ
```
指数衰减积分特征的物理意义：模拟不同时间尺度的先驱核/缓发中子记忆效应——近期扰动权重高，远期扰动指数衰减。

### 双流 LSTM 架构

| 分支 | 预测目标 | 物理角色 |
|------|---------|---------|
| 宏观流 (lstm_macro) | Mode 1（主振幅） | 全场能量水平的演化趋势 |
| 微观流 (lstm_micro) | Modes 2-8（高阶形状） | 空间通量分布的形态变化 |

### Mode 1 预测：趋势积分 + 残差补偿
```
δ(t) = fc_mode1_delta(lstm_out)    — 瞬时增量
trend(t) = cumsum(δ(t))             — 累积趋势（"苦力"，捕捉大尺度演化）
residual(t) = fc_mode1_residual(lstm_out) — 残差修正（"狙击手"，修正 cumsum 的细节误差）
pred_m1(t) = trend(t) + residual(t)
```
用 `cumsum` 而非直接回归 Mode 1 绝对值的原因：LSTM 直接学指数增长函数困难，但学恒定的增量 δ 容易得多。残差分支则捕捉 cumsum 无法表达的物理细节（初始过渡、扰动截止拐点等）。

### 高阶模态预测：形状比值
网络不直接输出 Modes 2-8 的绝对值，而是输出它们**相对于 Mode 1 的比值** R_k(t)：
```
pred_higher_k(t) = R_k(t) × pred_m1(t)
```
物理直觉：高阶模态的空间形状相对稳定（由反应堆几何决定），它们的绝对幅值天然跟随主振幅缩放。学习比值比学习绝对值更稳定。

### 数值技巧：SymLog 变换
对 POD 系数做 `sign(x) × log(1 + |x|)` 变换后再输入网络。目的：压缩指数爆发的动态范围，防止 L1/L2 损失在末端几个数量级变化下饱和。

## 三阶段训练策略

为避免不同模块的梯度互相干扰，采用分阶段冻结训练：

| 阶段 | Epoch | 解冻模块 | 训练目标 | 损失函数 |
|------|-------|---------|---------|---------|
| S1 | 0-30% | lstm_macro + fc_mode1_delta | 宏观趋势积分器 | Mode 1 的幅值加权 L1 |
| S2 | 30-85% | lstm_micro + fc_higher_modes | 高阶形状比值 | 比值 R_k × 1000 的 L1 |
| S3 | 85-100% | fc_mode1_residual | 残差精修 | Mode 1 的幅值加权 L1（lr=1e-4） |

**损失函数细节**：
- 时间加权：指数增长权重（末端 ×50），且前 5 步也保持 0.8×50（防止初期稳态被忽略）
- Stage 1/3：`L1(pred_m1, true_m1) × time_weight × (|true_m1| + 1.0)` — 幅值加权确保大信号不被小信号平均掩盖
- Stage 2：比值乘以 1000 放大尺度，保证 L1 梯度足够锋利

## 评估体系

- **POD 系数轨迹**（5.test.py）：逐 Mode 画真实 vs 预测时间序列，诊断各阶模态的预测质量
- **二维热力图**（6.validate.py）：空间分辨的真值 vs 预测 vs 绝对误差，定位空间误差热点
- **总功率守恒**（8.power-field.py）：对所有空间节点通量求和 → 全局功率演化曲线，特别关注末端指数爆发段的吻合度

## 目录结构

### 根目录 — 核心 ML 管道

- config.yaml — 全局配置（路径、物理参数、POD 维度、训练超参数）
- pyproject.toml — Python 项目依赖定义
- notes/ — 论文初稿与修改笔记

**核心管线 (`2D-PINN/`)**

1. 数据提取与降维
  - 1.data.py — 从 VTK 文件提取快/热中子通量（800 个空间节点），构建 6 维输入特征向量 [is_reg1, is_reg2, is_fast, is_thermal, p_t, t]，拼接指数衰减积分特征扩展至 10 维
  - 2.pod.py — 非中心化 TruncatedSVD POD 降维（快群 4 模态 + 热群 4 模态 = 8 维系数），记录方差占比与处理规模日志至 `2D-PINN/log/pod_processing_log.txt`
  - 7.pod-field.py — POD 空间模态可视化（平均场 + 各阶模态的空间形状）

2. 模型与训练
  - 3-model.py — 核心模型 POD-LSTM：双流 LSTM 架构，创新点在于振幅-形状分解——主模态（Mode 1）用"趋势积分 + 残差补偿"预测，高阶模态作为相对主模态的"形状比值"预测
  - 4.train.py — 三阶段训练策略：Stage 1 训练宏观趋势积分器 → Stage 2 训练微观高阶模态 → Stage 3 精修残差分支。使用指数时间加权 + SymLog 数值变换

3. 评估与可视化
  - 5.test.py — 全流程推理评估，输出 POD 系数轨迹对比图；诊断日志保存至 `2D-PINN/log/test_log_<timestamp>.txt`
  - 6.validate.py — 二维热力图对比（真值 vs 预测 vs 绝对误差）
  - 8.power-field.py — 反应堆总功率守恒对比（全堆通量求和，含指数爆发阶段放大）

4. 日志
  - log/ — 训练日志 (`training_log.csv`) 和测试日志 (`test_log_*.txt`) 统一存放

### 数据管线 (`data-pipeline/`)

1. 数据生成 (`data-pipeline/data-gen/`)
  - 1.generate_twigl_datasets.py — 用 LHS 采样生成 2500 个 TWIGL 算例的 .prm 输入文件，参数包括：扰动区域、扰动能群、扰动斜率 (slope_up)、截止时间 (cut_time)
  - 2.run_femffusion_cases.py — 批量调用 FEMFFUSION 求解器，对每个算例运行 101 个时间步（t=0 到 t=0.5s）的中子扩散模拟，输出 VTK 通量场
  - 3.filter_dataset.py — 过滤数值发散或无明显物理响应的算例

2. 数据处理 (`data-pipeline/`)
  - 3-data_split.py — 按 slope_up × cut_time 总扰动量排序，将参数空间边缘的 148 个算例作为外推测试集，其余随机分为训练集和验证集（150 个）
  - 4-data_norm.py — 对连续特征 MinMax 归一化，对离散特征（区域/能群）独热编码
  - data-raw/ — FEMFFUSION 原始输出（2500 算例的 VTK/out 文件）
  - data-split/ — 训练/验证/外推集 CSV
  - data-processed/ — 归一化后的中间数据
  - data-check-plots/ — 数据质量检查图
